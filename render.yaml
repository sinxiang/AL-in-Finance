services:
  - type: web
    name: al-in-finance-api
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn main:app --host 0.0.0.0 --port $PORT"
    envVars:
      - key: PORT
        value: "10000"   # Render 默认端口，你也可以留空让Render自动设置
